---
description: Push code changes to GitHub for Vercel deployment
---

// turbo-all

## Deploy to GitHub

1. Stage all changes:
```bash
git add -A
```

2. Commit with a descriptive message:
```bash
git commit -m "Your commit message here"
```

3. Push to main branch:
```bash
git push origin main
```
